import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MyFirstIonic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
